---
interface Props {
	pubDate: Date;
}

const { pubDate } = Astro.props;
---

<time datetime={pubDate.toISOString()}>
	{
		pubDate.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
